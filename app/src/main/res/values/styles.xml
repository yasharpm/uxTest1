<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/windowBackground</item>
    </style>

    <style name="ItemButton">
        <item name="android:textSize">@dimen/item_button_textsize</item>
        <item name="android:textColor">@color/item_button_textcolor</item>
        <item name="android:background">@drawable/item_button_background</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ItemText">
        <item name="android:textSize">@dimen/item_textsize</item>
        <item name="android:textColor">@color/item_textcolor</item>
        <item name="android:lineSpacingExtra">4dp</item>
    </style>

    <style name="ServiceItem">
        <item name="cardBackgroundColor">@color/windowBackground</item>
        <item name="cardCornerRadius">8dp</item>
    </style>
    
    <style name="ServiceItemText">
        <item name="android:textSize">13sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

</resources>
